const stages = ["QUOTE_SUBMITTED", "ARTWORK_REVIEW", "PROOF_PREPARATION", "AWAITING_CUSTOMER_APPROVAL", "PRODUCTION_PLANNING", "SUPPLIER_ORDER_PREPARATION", "IN_PRODUCTION", "READY", "COMPLETED"] as const;
const labels = ["Submitted", "Artwork", "Proof", "Approval", "Planning", "Supplier", "Production", "Ready", "Complete"];
export function WorkflowTracker({ current }: { current: string }) { const active = Math.max(0, stages.indexOf(current as typeof stages[number])); return <ol className="workflow-tracker" aria-label="Quote workflow progress">{stages.map((stage, index) => <li aria-current={index === active ? "step" : undefined} data-state={index < active ? "complete" : index === active ? "current" : "future"} key={stage}><span aria-hidden="true">{index < active ? "✓" : index + 1}</span><strong>{labels[index]}</strong><small>{index < active ? "Completed" : index === active ? "Current stage" : "Not started"}</small></li>)}</ol>; }
